# npm upgrade note

I upgraded the npm version in the development environment to 11.5.2 using:

```
npm install -g npm@11.5.2
```

This was performed in the dev container at `/workspaces/adios/frontend` and is a global environment change (not tracked by git). This file documents the change for reproducibility.
